pipeline {
    agent any 
    stages {
 
        stage('npm install'){
            steps {
                echo 'install'
                echo '******************************'  
                echo `${process.env.BUILD_NUMBER}`
            }
        }
    }
}
