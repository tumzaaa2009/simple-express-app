pipeline {
    agent any 
    stages {
 
        stage('npm install'){
            steps {
                echo 'install'
                echo '******************************'  
                echo '${env.REGISTRY_URL}'
            }
        }
    }
}
